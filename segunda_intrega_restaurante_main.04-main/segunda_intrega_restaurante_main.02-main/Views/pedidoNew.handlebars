
<h2>Faça Seu Pedido</h2>
<p>Utilize o campo de itens para descrever seu pedido baseado no cardápio.</p>

<form method="POST" action="/pedido/add">
    
    {{#if session.user.nome}}
        <input type="hidden" name="nome" value="{{session.user.nome}}">
        <p>Usuário logado: <strong>{{session.user.nome}}</strong></p>
    {{else}}
        <input type="text" name="nome" placeholder="Seu nome" required>
    {{/if}}

    <label for="itens">Itens do Pedido:</label>
    <textarea id="itens" name="itens" placeholder="Liste seus itens e quantidades (Ex: 1x Risoto de Camarão, 2x Bruschetta)" required rows="5"></textarea>

    <label for="total">Valor Total Estimado (R$):</label>
    <input type="number" id="total" name="total" placeholder="0.00" step="0.01" min="0.01" required>

    <button type="submit" class="btn btn-primary">Finalizar Pedido</button>
</form>